# @package _global_

defaults:
  - /experiment/real_world/lite6_maixsense_unsupervised_probe_diagnostics_control

experiment:
  config:
    monty_config:
      motor_system_config:
        policy_selector:
          policy:
            disable_orient_compensation_translation: true
    logging:
      run_name: real_world_lite6_maixsense_unsupervised_probe_rotation_only
